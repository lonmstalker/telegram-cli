# Work Journal Archive

Immutable rotated shard. Do not edit after creation.

## [2026-07-15] completed | W-20260715-095 | Реализован малый MCP protocol adapter

- Первый Tasks-пункт P8 закрыт: восемь MCP 2025-11-25-compatible tool schemas покрывают session, brokered auth metadata, on-demand schema, workflow, raw call и ordered events без 1010-tool каталога.
- Adapter переиспользует strict serde-контракт `DaemonRequest`; transport principal инъецируется отдельно и forged argument rejected. `auth.begin/status/wait` не имеют secret submission fields.
- Curated workflow оставляет TDJSON constructors в Rust core; только universal raw `call` принимает `@type` и всё равно проходит generated validation/policy daemon path.
- Contract: [D-20260715-089](../../decisions/decisions.md), [`docs/feature-logic-harness/mcp.md`](../../../docs/feature-logic-harness/mcp.md). Перед коммитом выполняется полный обязательный gate.
- Следующий Tasks-пункт P8: local stdio и authenticated remote transport.
